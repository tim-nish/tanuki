# Journey

Append-only record of the concrete episodes behind accepted lesson
candidates (spec-den-distill §4, issue #298). Entries are harvest pins:
appended by `tanuki-ledger distill` at accept time, one per accepted
candidate, and never edited retroactively.

Entry shape (one line each):
`- <date> — <episode: one atomic fact — an event, number, quote, or result>
(<finding id>[, <issue ids>]; lesson: <hub lesson slug>)`

<!-- entries below — append only -->
