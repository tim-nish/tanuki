# Runtime state lives in ~/.tanuki (outside this repo) and must never appear here.
__pycache__/
*.pyc
*.log
.DS_Store

# Owner-personal workflow config (untracked in 3054493) — never re-track.
.claude/*.json
